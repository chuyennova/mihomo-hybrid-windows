name: Test known-good v1.19.30 hybrid baseline

on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  baseline:
    uses: ./.github/workflows/build-tag.yml
    with:
      upstream_tag: v1.19.30
      release_revision: baseline-test
      publish_release: false
    permissions:
      contents: write
      issues: write
